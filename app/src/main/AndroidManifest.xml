<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayotomisin.clean_messy_predictor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clean_Messy_Predictor">
        <activity
            android:name=".ImagePredictActivity"
            android:label="@string/new_detection"
            android:theme="@style/Theme.Clean_Messy_Predictor.NoActionBar">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/clean_messy_detector"
            android:theme="@style/Theme.Clean_Messy_Predictor.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>